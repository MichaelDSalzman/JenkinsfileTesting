def slackLib

pipeline{
    agent any //TODO
  
    stages{
        stage("Init") {
            steps{
                echo "========Running init========"
                script {
                  nexusLib = load "ci/jenkins/nexus_repository.groovy"
                    a = nexusLib.construct()
                    echo "${a.getName()} - ${a.getType()}"                }
            }
        }
    }
}